package roofing.coffee.maker.components;

public class WaterReservoirTest {

    // Test that we stop brewing when coffeepot.isFull
    // We cannot test ths in the CoffeeMakerTest because we cannot manipulate
    // CoffeePot.MAX_CAPACITY_CUPS since it's not an app setting

}